import * as React from 'react';

class BlockChain extends React.Component {
  // finder = () => {
  //
  // }
  render() {
    return (
      <div>{'hello world'}</div>
    );
  }

}

export default BlockChain;
